version: 2

mkdocs:
  configuration: mkdocs.yml

formats: all

build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - dev
        - docs
